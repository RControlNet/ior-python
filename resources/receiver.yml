rcn:
  binders:
    message:
      enable: true
      default: mqtt
      brokerUrl: ${MQTTHOST}
      brokerPort: ${MQTTPORT}
      mqtt:
        consumer:
          mqtt-to-ior-channel:
            destination: rcn.elfie.ior.messages
