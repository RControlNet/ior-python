rcn:
  binders:
    message:
      enable: true
      default: mqtt
      brokerUrl: localhost
      brokerPort: 1883
      mqtt:
        consumer:
          mqtt-to-ior-channel:
            destination: rcn.elfie.ior.messages